<%# app/views/system/admin/group_nexts/new.html %>
<%
options = {
  'table_name'=>'system_group_updates',:action=>'index',
  'link_to_show'=>0
}
options['field_td_criteria']={
  '_parent_ou'=>Proc.new{|item|    item.parent_code+item.parent_name  } ,
  '_group_ou' =>Proc.new{|item|    item.code+item.name  }
}
%>
<%= table_to_index2 [@group_updates],options %>
<%#= table_to_show2 @group_updates,options %>

<% form_for :item, :url => {:action => :create} do |f| %>

<%= f.error_messages %>
<%= render :partial => 'form', :locals => {:f => f} %>

<%= submit_for_create f %>

<% end %>
