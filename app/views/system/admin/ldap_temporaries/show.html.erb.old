<% if @groups && @groups.size > 0 %>

<div style="margin: 10px 0px; font-size: 90%; font-family: 'MS Gothic';">
  <% @groups.each do |dep| %>
    <div style="background-color: #ddd; <%= 'color: red;' if dep[:duplicated] %>">
      <%= dep[:parent_id] %>,<%= dep[:id] %>,<%= dep[:name] %>,<%= dep[:name_en] %>,<%= dep[:email] %>
    </div>
    <% dep[:sections].each do |sec| %>
      <span style="<%= 'color: red;' if sec[:duplicated] %>">
        <%= sec[:parent_id] %>,<%= sec[:id] %>,<%= sec[:name] %>,<%= sec[:name_en] %>,<%= sec[:email] %>
      </span><br />
    <% end %>
  <% end %>
</div>

<% end %>

<% if @users && @users.size > 0 %>

<div style="margin: 10px 0px; font-size: 90%; font-family: 'MS Gothic';">
  <div style="background-color: #ddd;">
    ユーザー
  </div>
  <% @users.each do |user| %>
    <span style="<%= 'color: red;' if user[:duplicated] %>">
      <%= user[:group_id] %>,<%= user[:id] %>,<%= user[:name] %>,<%= user[:name_en] %>,<%= user[:email] %>
    </span><br />
  <% end %>
</div>

<% end %>
