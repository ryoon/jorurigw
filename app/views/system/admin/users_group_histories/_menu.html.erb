<%#= render :partial => 'system/admin/header' %>

<div class="menu">
<p>グループ内ユーザー</p>
<%= link_to '登録', new_system_users_group_history_path %>
<%= link_to '一覧', system_users_group_histories_path %>
<%= link_to 'CSV出力', csvput_system_users_group_histories_path %>
<%= link_to 'CSV登録', csvup_system_users_group_histories_path %>
</div>

<div class="navi">
<% tree = @parent.parent_tree; tree.each_with_index do |c, k|
%><%=link_to '('+c.code+')'+c.name, url_for(:action => :index, :parent => c )
%> <% if tree.size != k + 1 %>&gt;<% end %>
<% end %>

</div>
