<% form_for 'in', @item,
  :url  => pref_jurisdiction_path(@item),
  :html => {:method => :put} do |f| %>

<%= f.error_messages %>

<%= render :partial => 'form', :locals => {:f => f, :action => 'edit'} %>
  
<div class="submitters">
<%= f.submit "編集する" %>
</div>
  
<% end %>
