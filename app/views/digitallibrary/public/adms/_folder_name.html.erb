<div class="navi">
  <% unless @parent.blank? %>
    <% tree = @parent.parent_tree; tree.each_with_index do |c, k| %>
      <%=link_to c.title, url_for(c.link_list_path) if c.doc_type ==0 %><%=sprintf("/ ") if c.doc_type ==0%>
    <% end %>
  <% end %>
</div>
