<%
str_limit = ""
str_limit = "&limit=#{params[:limit]}" unless params[:limit].blank?
%>
<%=render :partial => '/digitallibrary/public/docs/search'%>
<% unless params[:state] == 'DATE'%>
<div class="sideMenu">
<%#if @title.importance == 0%>
<!--<div class="dateIndex"><%=link_to("日付順一覧", "#{@title.docs_path}&state=DATE#{str_limit}")%></div> -->
<%#end%>
<div id="sideList">
<ul>
<%if @title.left_banner.blank?%>
<!-- <li class="root"><%#=link_to h(@title.title), @title.docs_path %></li> -->
<%unless params[:fld] == 'fop' %>
<li class="all"><span><%=link_to("階層全開 ▼", "#{@title.docs_path}#{str_limit}&fld=fop")%></span></li>
<%else%>
<li class="close"><span><%=link_to("階層全閉 ▲", "#{@title.docs_path}#{str_limit}")%></span></li>
<%end%>
<%else%>
<li class="root"><a href ="<%=@title.docs_path%>"><%=@title.left_banner.to_s%></a></li>
<%unless params[:fld] == 'fop' %>
<li class="all"><span><%=link_to("階層全開 ▼", "#{@title.docs_path}#{str_limit}&fld=fop")%></span></li>
<%else%>
<li class="close"><span><%=link_to("階層全閉 ▲", "#{@title.docs_path}#{str_limit}")%></span></li>
<%end%>
<%end%>
<%=digitallibrary_folder_trees(@items) %>
</ul>
</div>
</div>

<%end%>