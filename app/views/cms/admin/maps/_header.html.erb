<%= render :partial => 'cms/admin/header' %>

<div class="menu">
<p>サイトマップ</p>
<%= link_to '登録', new_cms_map_path %>
<%= link_to '編集', cms_maps_path %>
</div>

<div class="navi">
<% tree = @parent.parent_tree; tree.each_with_index do |c, k|
%><%=link_to c.title, url_for(:action => :index, :parent => c )
%> <% if tree.size != k + 1 %>&gt;<% end %>
<% end %>

</div>
