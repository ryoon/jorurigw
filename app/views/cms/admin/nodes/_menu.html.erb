<%# app/views/cms/admin/nodes/_menu.html.erb %>
<div class="menu">
<p>ディレクトリ</p>
<%# if Site.user.has_priv?(:create, :item => @parent.concept) %>
<%= link_to '新規登録', new_cms_node_path  %>
<%= link_to '一覧表示', cms_nodes_path %>
</div>
<div class="navi">
<% tree = @parent.parent_tree; tree.each_with_index do |c, k|
%><%=link_to c.title, url_for(:action => :index, :parent => c )
%> <% if tree.size != k + 1 %>&gt;<% end %>
<% end %>
</div>
