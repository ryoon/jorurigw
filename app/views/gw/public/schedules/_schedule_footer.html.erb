<%
if !defined?(show_flg)
  show_flg = true
end
%>
<% if show_flg %>
<% if !@line_box.blank? %>
<%= hidden_field_tag('my_url', @schedule_move_url_directive.sub('%q', "#{@schedule_move_qs}")) %>
<% end %>
<% if !@line_box.blank? %>
<%= render(:partial => '/gw/public/schedules/schedule_line_box', :locals=>{:d=>d, :mode=>mode, :move_mode=>:footer}) %>
<% end %>
<% end %>
