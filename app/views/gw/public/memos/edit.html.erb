<%# app/views/gw/public/memos/edit.html.erb %>
<%
concat render(:partial => '/gw/public/memos/header')
header = <<-EOL
#{show_notice}
#{render :partial => '/gw/public/memos/piece_header'}
EOL
@content_for_form = ''
content_for :form do
  form_for :item, :url => "/gw/memos/#{@item.id}", :html => {:method => :put, :multipart => true, :id => 'itemForm'} do |f|
    concat <<-EOL
#{f.error_messages}
#{render(:partial => 'form', :locals => {:f => f})}
#{submit_for_update(f,:caption=>"送信する", :js_submitter => 'my_submit()') unless @item.is_finished}
EOL
  end
end
body = { :header=>memos_tabbox_struct(0, @qsa), :body => @content_for_form }
concat hhbff_struct(:memo, :form, :header=>header, :body=>body)
%>
