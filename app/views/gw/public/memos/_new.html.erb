<%
concat render(:partial => '/gw/public/memos/header')
header = <<-EOL
#{show_notice}
#{render :partial => '/gw/public/memos/piece_header'}
EOL
content_for :form do
  form_for :item, :url => '/gw/memos', :html => {:multipart => true, :id => 'itemForm'} do |f|
    concat <<-EOL
#{f.error_messages}
#{render(:partial => 'form', :locals => {:f => f})}
#{submit_for_create f, :caption=>"送信する",:js_submitter => 'my_submit()'}
EOL
  end
end
body = { :header=>memos_tabbox_struct(0, @qsa), :body => @content_for_form }
concat hhbff_struct(:memo, :form, :header=>header, :body=>body)
%>
