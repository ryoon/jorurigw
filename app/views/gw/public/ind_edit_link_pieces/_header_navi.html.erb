<div class="navi">
<span style="margin-right:3em;" class="Menu">
  <%= link_to '全一覧' , "#{Site.current_node.public_uri}list?pid=#{@parent.id}" %>
</span>
<% tree = @parent.parent_tree %>
<% tree.each_with_index do |c, k| %>
  <% next if k == 0 %>
  <% location = Gw.chop_with(Site.current_node.public_uri, '/') %>
  <%= link_to c.name, "#{location}?pid=#{c.id}" %>
  <% if tree.size != k + 1 %>&gt;<% end %>
<% end %>
</div>
