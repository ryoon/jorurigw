<p class="bgAsh" >コメント</p>
  <!--<%#if @item.one_line_note == 1%><%#1行コメント使用する%>
  <span class="btComment"><%#=link_to('コメント作成', "#{@item.new_comment_path}#{gwbbs_params_set}")%></span>
  <%#end%>-->
  <%#= %Q(<br /><div class="notice">#{flash[:notice]}</div>) if flash[:notice] %>
<%@i = 0%>
<% for item in @comments %>
 <%@i = @i + 1%>
  <%#if item.editor_id == Site.user.code%>
    <%#=link_to hbr(item.body), "#{item.edit_comment_path}#{gwbbs_params_set}"%>
  <%#else%>
  <p class="leftPad2">
    <%=item.body%>
  </p>
  <%#end%>
  <#%=@i%></td>
  <p class="leftPad1">
  <%=item.editordivision %>
  <%=' : ' %>
  <%=item.editor %>
  （<%= item.created_at.strftime('%Y-%m-%d %H:%M') %>）
  </p>
  <%if# is_comment_delete(@item.section_code, item.editor_id, @is_admin)%>
    <%#=link_to('削除', "#{item.delete_comment_path}#{gwbbs_params_set}", :confirm => '削除をすると元に戻せません。削除しますか', :method => :delete)%>
  <%#end%>
<% end %>
</table>
</div>