<table class="show">
 <tr>
  <th>【名称】<%= required('※') %></th>
  <td><%= f.text_field :title, :size=>"90" %></td>
 </tr>
 <tr>
  <th>【対象となるOS】<%= required('※') %></th>
  <td><%= f.text_field :inpfld_002, :size=>"90" %></td>
 </tr>
 <tr>
  <th colspan="2">【概要】<%= required('※') %></th>
  </tr>
 <tr>
  <td class="mceEditor" colspan="2"><%= f.text_area :body, :class => 'mceEditor', :style => 'width:100%' %></td>
 </tr>
</table>

<%if attach_use_old%>
  <%=gwboard_image_form(f, @item, 'item_body')%>
  <%=gwboard_attach_form(f, @item)%>
<%else%>
  <%=gwboard_attachments_form(f, @item)%>
<%end%>
