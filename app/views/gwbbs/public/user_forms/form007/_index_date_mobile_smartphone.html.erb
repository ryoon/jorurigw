<%# ＮＮ事業要綱等文書掲示板　国通知日順 -%>
<table class="index">
<%
  brk_code = ''
  for item in @items
    unless brk_code == item.latest_updated_at.strftime('%Y-%m-%d').to_s
      brk_code = item.latest_updated_at.strftime('%Y-%m-%d').to_s
%>
  <tr class="subIndex">
    <th><%=item.latest_updated_at.strftime('%Y年%m月%d日').to_s%></th>
    <th>区分</th>
  </tr>
<p class="bgAsh"></p>
<%  end%>
<tr class="article">
<td class="leftLine docTitle">
<%=link_to(gwbd_category_name(@d_categories,item.category1_id), "#{item.show_path}#{gwbbs_params_set}" )%></td>
<td><%=link_to(item.inpfld_002, "#{item.show_path}#{gwbbs_params_set}")%></td>
</tr><tr class="article">
<td colspan="2" class="leftLine group"><span><%=link_to(item.title, "#{item.show_path}#{gwbbs_params_set}")%></span></td>
</tr>
<%end%>
</table>
